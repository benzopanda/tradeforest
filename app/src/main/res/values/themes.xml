<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TradeForest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TradeForest" parent="Base.Theme.TradeForest" />

    <style name="CustomButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_margin">3dp</item>
        <item name="android:backgroundTint">@color/primary</item>
        <item name="cornerRadius">10dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="CustomInput" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name= "boxStrokeColor">@color/text_input_box_stroke</item>
        <item name= "boxStrokeWidth">2dp</item>
        <item name="android:layout_margin">3dp</item>
    </style>

</resources>